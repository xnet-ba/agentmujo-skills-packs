---
name: developer
description: Main Developer coordinator/orchestrator.
license: MIT
compatibility: OpenCode native Agent Skills; adapt actions to the target project, platform and available tools.
metadata:
  pack: agentmujo-developer-skills-pack
  version: "1.0.0"
---

# Developer

## Purpose
Main Developer coordinator/orchestrator.

Route development tasks to the smallest set of relevant developer skills and enforce inspect → plan → implement → test → validate → report.

## Routing
Use this coordinator to select the smallest set of specialized skills needed for the task. Avoid loading the whole pack when a focused combination is sufficient.

## Workflow
`discover → inspect → plan → execute → validate → report`
